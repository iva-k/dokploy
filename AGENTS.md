# Codex Instructions

## Repository instructions

- Use ASD-STE100 for project documentation and agent communication.
- Keep changes minimal, focused, complete, and free of placeholder code.
- Do not add comments that repeat the code or describe the change.

## Authority contract

- Mutable tasks, status, priority, ownership, dependencies, work notes, and closure are stored in Beads.
- Requirements, decisions, architecture, process gates, and release criteria remain in their durable documents.
- Source and configuration are the implementation authority. Tests, CI, review, and Git history are completion evidence.
- Start work with `bd prime` and `bd ready`; inspect and claim an issue before implementation. Record discovered work with `bd create` and close only after acceptance and validation pass.
- Keep secrets out of Beads. Use `bd remember` only for useful, non-secret operational knowledge.
- This checkout uses embedded Dolt, actor `codex`, `dolt.auto-commit: off`, and no configured Beads remote. Do not create commits, push, or configure remote synchronization without explicit authorization.
- PR and release checklists are process and validation gates. They are not a mutable task store.

Codex native Beads hooks are disabled because they fail this workspace's setup refresh. Run `bd prime` explicitly.

<!-- BEGIN BEADS CODEX SETUP: generated by bd setup codex -->
## Beads Issue Tracker

Use Beads (`bd`) for durable task tracking in repositories that include it. Use the `beads` skill at `.agents/skills/beads/SKILL.md` (project install) or `~/.agents/skills/beads/SKILL.md` (global install) for Beads workflow guidance, then use the `bd` CLI for issue operations.

### Quick Reference

```bash
bd ready                # Find available work
bd show <id>            # View issue details
bd update <id> --claim  # Claim work
bd close <id>           # Complete work
bd prime                # Refresh Beads context
```

### Rules

- Use `bd` for all task tracking; do not create markdown TODO lists.
- Run `bd prime` when Beads context is missing or stale. Native hooks are not enabled in this checkout.
- Keep persistent project memory in Beads via `bd remember`; do not create ad hoc memory files.

**Architecture in one line:** issues live in the local embedded Dolt DB; this checkout has no configured Beads remote; `.beads/issues.jsonl` is a passive export. See https://github.com/gastownhall/beads/blob/main/docs/SYNC_CONCEPTS.md for details and anti-patterns.
<!-- END BEADS CODEX SETUP -->
